import { CompareShell } from '@/components/playground/CompareShell';

export const metadata = {
  title: 'Compare Scenarios | Run-IQ',
  description: 'Compare two fiscal scenarios side by side with the PPE engine.',
};

export default function ComparePage() {
  return <CompareShell />;
}
